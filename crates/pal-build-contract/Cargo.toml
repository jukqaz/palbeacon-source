[package]
name = "pal-build-contract"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
publish = false
build = "build.rs"

[package.metadata.cargo-machete]
ignored = ["serde_json"] # Used by build.rs; cargo-machete does not inspect build scripts.

[build-dependencies]
serde_json.workspace = true
